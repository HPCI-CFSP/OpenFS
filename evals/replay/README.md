# Replay set

Add immutable Run IDs here only after a Run has been reviewed and selected as representative. Store large retrieval artifacts outside Git and identify them by digest. A replay comparison must explain output differences caused by model, prompt, tool, policy, or source changes.
